// callback

const numbers = [1, 2, 3, 4, 5];

// callback
const double = (n) => n * 2;

// Higher order function
const doubleNumbers = numbers.map(double);
// filter
// reduce
console.log(doubleNumbers);

// you wrote
function greet(name) {
    // doisghf
}

// greet()
function callHolder(callback) {
    // dsfdsjfdj
    // callback(parameters...)
}

callHolder(greet)
//
